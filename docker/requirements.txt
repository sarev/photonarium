# =============================================================================
# Photonarium Docker Dependencies
#
# Note: torch, torchvision, torchaudio are installed separately in the
# Dockerfile with a configurable index URL (CPU/CUDA variants).
#
# Note: facenet-pytorch is installed with --no-deps to avoid its overly
# strict version bounds on torch/numpy/pillow.
# =============================================================================

# OpenCLIP for image embeddings and semantic search
open_clip_torch

# Image processing
pillow
opencv-python-headless
imagehash

# Core utilities
numpy
pyyaml

# Flask web server
flask
waitress
orjson
requests

# Image captioning (BLIP/BLIP-2)
transformers

# Camera RAW support
rawpy
exifread
