# Packages for the base image that have NO numpy dependency.
# Anything that depends on numpy must go in requirements-ml.txt
# to avoid Docker union filesystem conflicts (base layer's numpy .so
# files leak through alongside the variant layer's, causing
# "cannot load module more than once per process" at runtime).

pyyaml
flask
waitress
orjson
requests
exifread
